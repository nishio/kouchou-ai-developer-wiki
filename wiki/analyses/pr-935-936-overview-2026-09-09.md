---
type: analysis
summary: "PR #935はデータ非依存の静的viewer基盤、#936はexportのOGP説明訂正。通常PR2件とdraft #891を区別し、未対応範囲を整理"
sources:
  - https://github.com/digitaldemocracy2030/kouchou-ai/pull/935
  - https://github.com/digitaldemocracy2030/kouchou-ai/pull/936
---

## 観測

2026-09-09 15:45 JSTにGitHubのopen PRを確認。通常PRは#935 / #936、draftは#891。mainはfetch / pull後 `c2390a1`。以下はPR本文・差分・CIとローカルコードの読解による整理で、ローカルのビルド・ブラウザ再検証は未実施。

## #935: データ非依存の静的出力

[PR #935](https://github.com/digitaldemocracy2030/kouchou-ai/pull/935) HEAD `373c76b9583df8d6a66dc4ba738b7515bd090356` より、従来はレポートごとにNextビルドが必要だった静的出力へ、共通HTMLを一度生成し、配布時に各slugへコピーしてJSONを同梱するshell方式を追加。ブラウザがURLからslugを求めてJSONを読み込む。明示的に有効化した場合のみ使う。

Windows配布などでレポート出力のたびにNodeを必要としない構造へ進む基盤。ただし今回の範囲はviewerで、FastAPI側の配布物生成とstatic-site-builderの退役は未実装。[[issue-885-node-runtime-next-scope-2026-06-30]]の後続作業として読む。

GitHub上のClient Tests / E2E / client build / CodeQLは成功。既存レビューで指摘された失敗ファイルURLの表示は最新HEADで修正済み。作者明記の制約は、レポート別title・unlistedのnoindex未対応、basePath付きshellとレポート間遷移のE2E未検証。これらを把握したオプトイン基盤としての評価と、配布機能全体の完成を区別する。

## #936: OGPの説明訂正

[PR #936](https://github.com/digitaldemocracy2030/kouchou-ai/pull/936) HEAD `2c42c3008b76399bc8a0077eeeb7dcafabc492ec` より、静的export時にOGP画像を生成するという既存ドキュメントを訂正。mainのrename-file.mjsでも、export時にOGPの両経路を除外することを確認した。リンク共有時のプレビュー画像の生成機能を直すPRではなく、既存挙動を正しく説明するdocs-only変更。docs build成功、review required。#935とは別に判断できる。

## Open Questions

- #935の実配布への接続時に、title / noindexと配下パス・ページ遷移をどの段階で検証・対応するか。
- #885全体やdraft #891の完成とは別の段階であり、今回2件だけでWindows配布完成と見なさない。

## Updates

- 2026-09-09 15:45: ユーザー依頼で2件の内容とCIを確認。PRへの投稿・変更・マージは実施していない。
